# Simple personal page builded on Hugo with [Coder](https://github.com/luizdepra/hugo-coder/) theme

### Push changes
``` bash
# into source folder
sudo chmod +x deploy.sh
sudo ./deploy.sh "commit message here"
```

### Run local
``` bash
hugo server -D
```
